apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "frontend.fullname" . }}-nginx-config
  labels:
    {{- include "frontend.labels" . | nindent 4 }}
data:
  nginx.conf: {{ .Values.frontend.nginxConfig | quote }}
